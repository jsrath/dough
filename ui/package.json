{
  "name": "financials-ui",
  "version": "1.0.0",
  "description": "UI for the Financials App",
  "main": "index.html",
  "scripts": {
    "start": "npm run build --noEmit --watch & npm run compile index.html",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "compile": "parcel"
  },
  "author": "jsrath",
  "license": "ISC",
  "dependencies": {
    "@types/d3": "^5.7.2",
    "d3": "^5.16.0",
    "parcel-bundler": "1.12.4",
    "typescript": "^3.9.3"
  }
}
